package exps

import app1.SensorData

trait DataToMap {

  /*def DataToMapValues(input: Order): Map[String, Any] = {
    val fieldNames = input.getClass.getDeclaredFields.map(_.getName)
    val fieldVals = (input).get.productIterator.toSeq
    fieldNames.zip(fieldVals) toMap

    input.i
  }*/



}
